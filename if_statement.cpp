//write a program to show the simple example of if statement
#include<iostream>
using namespace std;
int main()
{
    int atm;
    cout<<"Enter atm pin\n";
    cin>>atm; //we have entered our atm pin here in output area it will responsible for that.
    if(atm=1234)
    {
        cout<<("you can access your balance.");
        
    }
    return 0;
}