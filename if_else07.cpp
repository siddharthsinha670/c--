//Check if two numbers are equal or not.

#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cout<<("Enter the number:");
    cin>>a>>b;
    if(a==b)
    {
        cout<<("This number is equal.");
    }
    else
    {
        cout<<("this number is not equal.");
    }
    return 0;
}