//Write a program to check if a number is zero.
#include<iostream>
using namespace std;
int main()
{
    int a;
    cout<<("Enter the number--");
    cin>>a;
    if(a==0)
    {
        cout<<("this is zero.");
    }
    return 0;
}