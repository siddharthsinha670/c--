//Write a program to check if a number is negative.
#include<iostream>
using namespace std;
int main()
{
    int a;
    cout<<("Enter the number:");
    cin>>a;
    if(a<=0)
    {
        cout<<("the number is negative.");
    }
    return 0;
}